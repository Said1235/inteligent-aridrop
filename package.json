{
  "name": "intelligent-airdrop",
  "version": "1.0.0",
  "description": "IntelligentAirdrop — evidence-based eligibility oracle on GenLayer (Studio Next). Deploy the contract with the GenLayer CLI (`genlayer network`, then `genlayer deploy`, which runs deploy/deployScript.ts) — see README.md.",
  "scripts": {
    "dev": "npm --prefix frontend run dev",
    "build": "npm --prefix frontend run build"
  },
  "devDependencies": {
    "genlayer-js": "2.0.0-rc.1",
    "typescript": "^5.9.3"
  }
}
